# شرح الشروط في لغة رمز - Conditions in Ramz Programming Language

## نظرة عامة

تقدم لغة رمز نظام شروط متكامل لتقييم التعبيرات المنطقية واتخاذ قرارات البرنامج. يدعم اللغة جميع عمليات المقارنة الأساسية والعوامل المنطقية مع دعم قوي لأنواع البيانات المختلفة.

## ١. عوامل المقارنة (Comparison Operators)

تدعم لغة رمز جميع عوامل المقارنة الأساسية:

### عوامل المقارنة المدعومة

| العامل | الاسم | المعنى | مثال |
|--------|-------|---------|------|
| `==` | يساوي (Equal) | التحقق من تساوي القيمتين | `العمر == 18` |
| `!=` | لا يساوي (Not Equal) | التحقق من عدم تساوي القيمتين | `الاسم != "علي"` |
| `>` | أكبر من (Greater Than) | التحقق من أن القيمة الأولى أكبر | `الدرجة > 80` |
| `<` | أصغر من (Less Than) | التحقق من أن القيمة الأولى أصغر | `العدد < 100` |
| `>=` | أكبر من أو يساوي (Greater or Equal) | التحقق من أن القيمة الأولى أكبر أو تساوي | `العمر >= 18` |
| `<=` | أصغر من أو يساوي (Less or Equal) | التحقق من أن القيمة الأولى أصغر أو تساوي | `الدرجة <= 100` |

### تنفيذ المقارنات في الكود المصدري

```rust
// من ملف interpreter.rs - مقارنة الأرقام
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::Equal) => {
    Ok(RamzValue::Boolean(l == r))
}
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::NotEqual) => {
    Ok(RamzValue::Boolean(l != r))
}
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::LessThan) => {
    Ok(RamzValue::Boolean(l < r))
}
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::LessEqual) => {
    Ok(RamzValue::Boolean(l <= r))
}
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::GreaterThan) => {
    Ok(RamzValue::Boolean(l > r))
}
(RamzValue::Number(l), RamzValue::Number(r), BinaryOperator::GreaterEqual) => {
    Ok(RamzValue::Boolean(l >= r))
}

// مقارنة النصوص
(RamzValue::String(l), RamzValue::String(r), BinaryOperator::Equal) => {
    Ok(RamzValue::Boolean(l == r))
}

// مقارنة القيم المنطقية
(RamzValue::Boolean(l), RamzValue::Boolean(r), BinaryOperator::Equal) => {
    Ok(RamzValue::Boolean(*l == *r))
}
```

### أمثلة على المقارنات

```ramz
# مقارنة الأرقام
متغير x = 10
متغير y = 20

# عمليات مقارنة صحيحة
إذا x < y:
    اكتب("x أصغر من y")

إذا x != y:
    اكتب("x لا يساوي y")

إذا x >= 10:
    اكتب("x أكبر من أو يساوي 10")

# مقارنة النصوص
مت convier اسم = "أحمد"
إذا اسم == "أحمد":
    اكتب("مرحباً أحمد!")

إذا اسم != "محمد":
    اكتب("أنت لست محمد!")

# مقارنة القيم المنطقية
متغير نشط = صحيح
إذا نشط:
    اكتب("الحساب نشط")

إذا !نشط:
    اكتب("الحساب غير نشط")
```

## ٢. عوامل التشغيل المنطقية (Logical Operators)

تدعم لغة رمز العوامل المنطقية الأساسية:

### العوامل المنطقية المدعومة

| العامل | الاسم | المعنى | مثال |
|--------|-------|---------|------|
| `&&` | AND (و) | صحيح إذا كانت كل المتغيرات صحيحة | `العمر >= 18 && له_رخصة` |
| `||` | OR (أو) | صحيح إذا كان أي متغير صحيح | `الطقس == "مشمس" || الحرارة >= 20` |
| `!` | NOT (ليس) | عكس القيمة المنطقية | `!متعب` |

### تنفيذ العوامل المنطقية في الكود المصدري

```rust
// AND operation
(RamzValue::Boolean(l), RamzValue::Boolean(r), BinaryOperator::And) => {
    Ok(RamzValue::Boolean(*l && *r))
}

// OR operation
(RamzValue::Boolean(l), RamzValue::Boolean(r), BinaryOperator::Or) => {
    Ok(RamzValue::Boolean(*l || *r))
}

// NOT operation
(UnaryOperator::Not, RamzValue::Boolean(b)) => Ok(RamzValue::Boolean(!b))
```

### ملاحظة هامة: Arabic Logical Operators

بين أن الدروس والتوثيق تستخدم الكلمات العربية للعوامل المنطقية:

```ramz
# مثال من الدرس
إذا العمر >= 18 و له_رخصة:
    اكتب("يمكنك قيادة السيارة!")
```

**تنفيذ الكود المصدري يدعم فقط النموذج الإنجليزي**:
- `&&` بدلاً من `و`
- `||` بدلاً من `أو` 
- `!` بدلاً من `ليس`

### أمثلة على العوامل المنطقية

```ramz
# استخدام AND (&&)
متغير العمر = 20
متغير له_رخصة = صحيح

إذا العمر >= 18 && له_رخصة:
    اكتب("يمكنك قيادة السيارة!")

# استخدام OR (||)
متغير الطقس = "مشمس"
متغير الحرارة = 25

إذا الطقس == "مشمس" || الحرارة >= 20:
    اكتب("وقت مثالي للخروج!")

# استخدام NOT (!)
متغير متعب = صحيح
إذا !متعب:
    اكتب("أنت لست متعباً!")

# التجميع المعقد
إذا (العمر >= 18 && له_رخصة) || (العمر >= 21):
    اكتب("يمكنك القيادة!")
```

## ٣. أنواع البيانات المدعومة في الشروط

تدعم لغة رمز المقارنة بين الأنواع التالية:

### أرقام (Numbers)

```ramz
متغير x = 10
متغير y = 20

# جميع عمليات المقارنة تعمل
إذا x < y:
    اكتب("x أصغر من y")

إذا x != y:
    اكتب("x لا يساوي y")

إذا x >= 10:
    اكتب("x أكبر من أو يساوي 10")
```

### عشرات (Floats)

```ramz
متغير pi = 3.14
متغير e = 2.71

إذا pi > e:
    اكتب("pi أكبر من e")

إذا pi != e:
    اكتب("pi لا يساوي e")
```

### نصوص (Strings)

```ramz
متvrier اسم = "أحمد"
متvrier لقب = "محمد"

إذا اسم == "أحمد":
    اكتب("مرحباً أحمد!")

إذا اسم != لقب:
    اكتب("الاسم واللقب مختلفان")

# مقارنة حساسة لحالة الأحرف
إذا اسم == "احمد":  # خطأ - الأحرف مختلفة
    اكتب("مرحباً احمد!")
```

### قيم منطقية (Booleans)

```ramz
متغير نشط = صحيح
متغير متعب = خطأ

# القيمة المنطقية نفسها يمكن أن تكون شرطاً
إذا نشط:
    اكتب("الحساب نشط")

إذا متعب:
    اكتب("أنت متعب!")

# مع العوامل المنطقية
إذا نشط && !متعب:
    اكتب("أنت نشيط وغير متعب")
```

### قوائم وقواميس (Lists and Dictionaries)

```ramz
متvrier الأسماء = ["أحمد", "محمد", "خالد"]
متvrier الشخص = {"الاسم": "أحمد", "العمر": 20}

# التحقق من وجود عنصر في القائمة
إذا "أحمد" في الأسماء:
    اكتب("أحمد موجود في القائمة!")

# التحقق من القيم في القاموس
إذا الشخص["العمر"] >= 18:
    اكتب("شخص بالغ")
```

## ٤. بنية الجملة الشرطية

### جملة if (إذا)

البنية الأساسية للشروط:

```ramz
إذا الشرط:
    # الكود المنفذ إذا كان الشرط صحيحاً
```

### مثال بسيط

```ramz
متغير العمر = 20

إذا العمر >= 18:
    اكتب("أنت بالغ")
```

### جملة if-else (إذا-وإلا)

معالجة الحالة البديلة:

```ramz
إذا الشرط:
    # الكود إذا كان الشرط صحيحاً
وإلا:
    # الكود إذا كان الشرط خاطئاً
```

### مثال

```ramz
متغير العمر = 15

إذا العمر >= 18:
    اكتب("أنت بالغ")
وإلا:
    اكتب("ما زلت طفلاً")
```

### جملة if-else if-else (إذا-وإلا إذا-وإلا)

معالجة شروط متعددة:

```ramz
إذا الشرط1:
    # الكود إذا كان الشرط1 صحيحاً
وإلا إذا الشرط2:
    # الكود إذا كان الشرط2 صحيحاً
وإلا إذا الشرط3:
    # الكود إذا كان الشرط3 صحيحاً
وإلا:
    # الكود إذا كانت جميع الشروط خاطئة
```

### مثال عملي

```ramz
متvrier الدرجة = 85

إذا الدرجة >= 90:
    اكتب("ممتاز! 🌟")
وإلا إذا الدرجة >= 80:
    اكتب("جيد جداً! 👍")
وإلا إذا الدرجة >= 70:
    اكتب("جيد! 👌")
وإلا إذا الدرجة >= 60:
    اكتب("مقبول! 📝")
وإلا:
    اكتب("راسب! ❌")
```

## ٥. أولوية العمليات

تتبع لغة رمز الأولوية الرياضية القياسية في الشروط:

1. `!` (NOT) - الأولوية القصوى
2. `&&` (AND) - الأولوية المتوسطة
3. `||` (OR) - الأولوية الدنيا

### أمثلة على الأولوية

```ramz
# هذه الجملة تقرأ كـ: (A && B) || C
إذا A && B || C:
    اكتب("الشرط صحيح")

# هذه الجملة تقرأ كـ: A && (B || C)
إذا A && (B || C):
    اكتب("الشرط صحيح")

# استخدام الأقواس لتغيير الأولوية
إذا !(A && B):
    اكتب("ليس (A و B)")
```

## ٦. أمثلة متقدمة

### مثال 1: نظام تسجيل دخول

```ramz
اكتب("=== نظام تسجيل الدخول ===")

متvier الاسم = اقرأ("اسم المستخدم: ")
متvier كلمة_المرور = اقرأ("كلمة المرور: ")

إذا الاسم == "admin" && كلمة_المرور == "123456":
    اكتب("✅ تسجيل دخول ناجح!")
    اكتب("مرحباً " + الاسم + "! 🎉")
وإلا:
    اكتب("❌ اسم المستخدم أو كلمة المرور خاطئة!")
```

### مثال 2: آلة حاسبة ذكية

```ramz
اكتب("=== آلة حاسبة ذكية ===")

متvier الرقم1 = اقرأ("الرقم الأول: ")
متvier الرقم2 = اقرأ("الرقم الثاني: ")
اكتب("اختر العملية (+، -، *، /): ")
متvier العملية = اقرأ()

إذا العملية == "+":
    اكتب("النتيجة: " + (الرقم1 + الرقم2))
وإلا إذا العملية == "-":
    اكتب("النتيجة: " + (الرقم1 - الرقم2))
وإلا إذا العملية == "*":
    اكتب("النتيجة: " + (الرقم1 * الرقم2))
وإلا إذا العملية == "/":
    اكتب("النتيجة: " + (الرقم1 / الرقم2))
وإلا:
    اكتب("❌ عملية غير صالحة!")
```

### مثال 3: تحديد الفصل الدراسي

```ramz
اكتب("=== تحديد الفصل الدراسي ===")

متvier العمر = اقرأ("ما عمرك؟ ")

إذا العمر < 6:
    اكتب("👶 الروضة")
وإلا إذا العمر < 12:
    اكتب("📚 المرحلة الابتدائية")
وإلا إذا العمر < 15:
    اكتب("📖 المرحلة الإعدادية")
وإلا إذا العمر < 18:
    اكتب("🎓 المرحلة الثانوية")
وإلا:
    اكتب("👨‍🎓 الجامعة أو العمل")
```

## ٧. نصائح مهمة

### ١. استخدم الأقواس للوضوح

```ramz
# جيد
إذا (العمر >= 18 && له_رخصة) || له_إعفاء:
    اكتب("يمكنك القيادة!")

# سيء - صعب القراءة
إذا العمر >= 18 && له_رخصة || له_إعفاء:
    اكتب("يمكنك القيادة!")
```

### ٢. اختبر جميع الحالات

```ramz
# تأكد من اختبار:
# - الشرط صحيح
# - الشرط خاطئ
# - الحالات المتطرفة

# مثال: اختبار مقارنة النصوص
متvier اسم = "أحمد"
إذا اسم == "أحمد":      # حالة صحيحة
    اكتب("مرحباً أحمد!")
إذا اسم == "احمد":      # حالة خاطئة (الأحرف)
    اكتب("مرحباً احمد!")
إذا اسم != "محمد":      # حالة مختلفة
    اكتب("أنت لست محمد!")
```

### ٣. استخدم متغيرات ذات أسماء واضحة

```ramz
# جيد
متvier عمر_الطالب = 18
متvier رصيد_الحساب = 1000.50
متvier حساب_نشط = صحيح

إذا عمر_الطالب >= 18 && رصيد_الحساب > 0 && حساب_نشط:
    اكتب("يمكنك الشراء!")

# سيء
متvier x = 18
متvrier y = 1000.50
متvrier z = صحيح

إذا x >= 18 && y > 0 && z:
    اكتب("يمكنك الشراء!")
```

### ٤. احتفظ بالتباعد (Indentation)

```ramz
# جيد
إذا العمر >= 18:
    اكتب("أنت بالغ")
    اكتب("يمكنك التصويت")
وإلا:
    اكتب("أنت قاصر")
    اكتب("لا يمكنك التصويت")

# سيء - بدون تباعد
إذا العمر >= 18:
اكتب("أنت بالغ")
اكتب("يمكنك التصويت")
وإلا:
اكتب("أنت قاصر")
اكتب("لا يمكنك التصويت")
```

## ٨. أخطاء شائعة

### ١. استخدام `=` بدلاً من `==` في المقارنة

```ramz
# خطأ
إذا العمر = 18:  # يجب == وليس =
    اكتب("أنت بالغ")

# صحيح
إذا العمر == 18:
    اكتب("أنت بالغ")
```

### ٢. نسيان الأقواس في التجميع المعقد

```ramz
# خطأ - الأولوية غير صحيحة
إذا العمر >= 18 || العمر < 13 && له_رخصة:
    اكتب("شرط معقد")

# صحيح - استخدام الأقواس للتحكم في الأولوية
إذا (العمر >= 18) || (العمر < 13 && له_رخصة):
    اكتب("شرط معقد")
```

### ٣. مقارنة أنواع مختلفة

```ramz
# خطأ - محاولة مقارنة نص ورقم
إذا "10" == 10:
    اكتب("الشرط صحيح")

# صحيح - تحويل نوع البيانات أولاً
إذا "10" == str(10):
    اكتب("الشرط صحيح")
```

## ٩. ملخص

تدعم لغة رمز نظام شروط متكامل يشمل:

- ✅ **عوامل المقارنة**: `==`, `!=`, `<`, `>`, `<=`, `>=`
- ✅ **عوامل التشغيل المنطقية**: `&&` (AND), `||` (OR), `!` (NOT)
- ✅ **دعم الأنواع**: الأرقام، العشرات، النصوص، القيم المنطقية، القوائم، القواميس
- ✅ **البنية الشرطية**: `إذا`, `وإلا`, `وإلا إذا`
- ✅ **أولوية العمليات**: ! > && > ||
- ✅ **دعم التجميع**: استخدام الأقواس لتغيير الأولوية

### أفضل الممارسات

1. استخدم أسماء متغيرات واضحة وصعبة القراءة
2. استخدم الأقواس في التجميع المعقد
3. اختبر جميع الحالات المحتملة
4. احتفظ بالتباعد المناسب لتحسين القراءة
5. تجنب مقارنة أنواع بيانات مختلفة مباشرة

مع هذه الأدوات، يمكنك بناء برامج ذكية تستجيب لظروف مختلفة وتتخذ قرارات منطقية معقدة! 🎯